# infrastructure

Reserved for deployment infrastructure (Dockerfiles for `apps/api` and
`apps/web`, CI pipelines, IaC) once the project actually deploys somewhere.

Local development infrastructure (Postgres, Redis) is defined in the root
`docker-compose.yml`, not here — this folder is specifically for things
beyond local dev.
