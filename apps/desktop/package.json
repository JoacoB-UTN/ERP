{
  "name": "@erp/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "ERP thin Electron client — connects to a server-hosted ERP Server, never bundles Gestión/Facturación/apps/api.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\"",
    "test": "vitest run",
    "package": "npm run build && electron-builder --win --x64 --publish never"
  },
  "devDependencies": {
    "@erp/eslint-config": "*",
    "@erp/typescript-config": "*",
    "@types/node": "^24.0.0",
    "electron": "44.0.0",
    "electron-builder": "^26.15.3",
    "eslint": "^9.18.0",
    "globals": "^17.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22.12"
  }
}
