# organizations

Reserved for the future `organizations` domain module (see `CLAUDE.md` → Architecture
→ Expected modules).

No business logic lives here yet — this task ("ERP Foundation") intentionally
implements only the multi-tenancy skeleton (Tenant/Company/Branch/User/UserCompany)
and cross-cutting infrastructure (config, database, redis, health).
