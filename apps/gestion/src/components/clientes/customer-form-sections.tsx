// Re-exported from the shared primitive — see components/ui/form-section.tsx.
// Kept as a distinct module so existing customer-page imports don't need
// to change; the component itself has no customer-specific logic.
export { FormSection, FieldError } from '@/components/ui/form-section';
